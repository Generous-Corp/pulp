diff --git a/include/forge/chrome.hpp b/include/forge/chrome.hpp
index 0863f43..a6c7e89 100644
--- a/include/forge/chrome.hpp
+++ b/include/forge/chrome.hpp
@@ -640,6 +640,38 @@ public:
         return generation_turn_state_.has_active();
     }
     int chat_line_count() const { return chat_line_count_; }
+
+    /// Swap the materializing skeleton for the product's artifact view, or
+    /// back. A product with no stage accessory is unaffected.
+    void show_stage_accessory(bool shown) {
+        if (!stage_accessory_) return;
+        stage_accessory_->set_visible(shown);
+        if (skeleton_) skeleton_->set_visible(!shown);
+    }
+    pulp::view::View* stage_accessory() const { return stage_accessory_; }
+    pulp::view::View* build_accessory() const { return build_accessory_; }
+
+    /// Begin a fresh session: empty transcript, no carried-over artifact.
+    ///
+    /// A prompt typed on Home is a NEW thing. Appending it to whatever was last
+    /// open makes two unrelated builds share one conversation, and the second
+    /// one inherits the first one's stage.
+    void begin_new_session() {
+        clear_chat_rail();
+        show_stage_accessory(false);
+    }
+
+    /// Put one line of narration in the Build transcript.
+    ///
+    /// The seam a shell uses to report what its own generator is doing. Forge's
+    /// own products narrate through the agent loop, which owns the private
+    /// append path; a shell that drives an external tool has no loop to speak
+    /// through, and would otherwise have to build a second transcript beside
+    /// this one. `alarming` marks the two a person must not scroll past: a
+    /// refusal and a hard error.
+    void narrate(const std::string& text, bool alarming = false) {
+        append_agent_line(text, alarming);
+    }
     // The text of every agent line shown on the chat rail, in order (tests:
     // assert the user-facing rail stays calm — no raw parser/gate reject text).
     const std::vector<std::string>& agent_lines() const { return agent_lines_; }
@@ -712,6 +744,16 @@ public:
     std::string first_tile_badge_text() const;
     bool delete_affordance_visible() const;
     const std::string& project_title() const { return project_title_; }
+
+    /// Name the thing being built, from what was asked for.
+    ///
+    /// The title otherwise stays at whatever placeholder the product declared,
+    /// so a session building a random module still reads "FORGING · WAVEFOLDER".
+    /// A wrong name is worse than a generic one: it says the app is showing you
+    /// someone else's work.
+    /// Name the session from a prompt, using the same helper Forge's own
+    /// products use so all four title the same way.
+    void set_project_title_from_prompt(const std::string& prompt);
     // The FIRST shelf tile — the live build's (tests: drive its hover reveal
     // without coordinates).
     pulp::view::View* shelf_tile() { return shelf_tile_; }
@@ -722,6 +764,14 @@ public:
     // The Home prompt box and the Build rail's follow-up box (tests: pin that
     // their copy is the hosting shell's kind, not effect wording everywhere).
     pulp::view::TextEditor* prompt_input() { return prompt_input_; }
+
+    /// Re-read the shell's copy and update what is already on screen.
+    ///
+    /// ChromeCopy is read once while building. A product whose copy changes at
+    /// runtime -- Forge Modular's Module/Patch tabs change every string on the
+    /// home screen -- needs the visible labels refreshed without rebuilding the
+    /// whole tree, which would drop focus and any typed prompt.
+    void refresh_copy();
     pulp::view::TextEditor* followup_input() { return followup_input_; }
     std::vector<std::string> followup_suggestions() const;
     // The hover tooltip's text, or "" when it is not showing. A clamped chip
@@ -1133,6 +1183,8 @@ private:
 
     bool materialized_ = false;
     int chat_line_count_ = 0;
+    pulp::view::View* build_accessory_ = nullptr;
+    pulp::view::View* stage_accessory_ = nullptr;
     std::vector<std::string> agent_lines_;  // agent rail text, for introspection
 
     // The ordered build-session transcript. A `user` turn is a prompt bubble;
@@ -1171,6 +1223,7 @@ private:
 
     // Non-owning pointers into the built tree (valid while the root lives).
     pulp::view::View* root_ = nullptr;
+    pulp::view::View* overlay_accessory_ = nullptr;
     pulp::view::View* title_bar_ = nullptr;
     pulp::view::View* shell_body_ = nullptr;
     pulp::view::View* shell_rail_ = nullptr;
diff --git a/include/forge/shell.hpp b/include/forge/shell.hpp
index c8101e1..433eb33 100644
--- a/include/forge/shell.hpp
+++ b/include/forge/shell.hpp
@@ -59,6 +59,55 @@ class ProjectStore;
 // re-bake into an effect shell), and used to select the generation vocabulary.
 enum class ShellKind { effect, instrument, midi_effect };
 
+/// One button in the composer's action row.
+///
+/// A description rather than a built widget, so a product can say what its row
+/// contains without the chrome hard-coding it and without the product reaching
+/// into chrome's view types.
+struct ComposerAction {
+    /// Which of chrome's icons to draw. `none` leaves the button text-only.
+    enum class Icon { none, plus, dice, arrow_up, document, search, gear };
+    /// Empty means icon-only, which is how the compact tiles are expressed.
+    std::string label;
+    /// What a screen reader says. Required -- an icon-only button with no label
+    /// is unreachable, and defaulting it to the visible text would leave the
+    /// icon-only cases silent.
+    std::string access_label;
+    Icon icon = Icon::none;
+    /// `primary` is the filled accent treatment; everything else is ghost.
+    bool primary = false;
+    /// Runs on click. A null callback means the button is present but inert,
+    /// which some are before their feature lands.
+    std::function<void()> on_click;
+};
+
+/// The composer's action row: what sits left of the spacer, and what sits right.
+struct ComposerRow {
+    std::vector<ComposerAction> left;
+    std::vector<ComposerAction> right;
+};
+
+/// The product-facing words the chrome shows.
+///
+/// These used to be four `switch (kind)` functions inside chrome.cpp, which put
+/// product copy in the layout code and meant a new product could not supply its
+/// own without editing the chrome. Each shell answers for itself now; the chrome
+/// asks rather than decides.
+struct ChromeCopy {
+    /// The title-bar badge — "FORGE INST", "FORGE MIDI", "FORGE FX".
+    std::string badge;
+    /// What the empty composer suggests.
+    std::string prompt_placeholder;
+    /// What the composer suggests once something has been built.
+    std::string followup_placeholder;
+    /// The name of the build a fresh install opens with.
+    std::string default_build_title;
+    /// The small mono line above the hero question.
+    std::string hero_eyebrow;
+    /// The hero question itself.
+    std::string hero_title;
+};
+
 const char* shell_kind_name(ShellKind kind);        // "effect" / "instrument" / "midi_effect"
 ShellKind shell_kind_from_name(const std::string& name, ShellKind fallback);
 
@@ -343,6 +392,78 @@ public:
     // ── Kind-specific hooks each concrete shell fills in ─────────────────────
 
     // Whether a build is installed and processing.
+    /// The words this product shows in the chrome.
+    ///
+    /// Pure virtual on purpose: a new product that forgets to answer will not
+    /// compile, rather than silently inheriting another product's copy.
+    virtual ChromeCopy chrome_copy() const = 0;
+
+    /// The composer's action row for this product.
+    ///
+    /// Returning an empty row means "the standard one", which is what the three
+    /// original products want and keeps this from being a breaking change for
+    /// anything that does not care.
+    virtual ComposerRow composer_row() { return {}; }
+
+    /// An extra view above the composer on Home, or nullptr for none.
+    ///
+    /// Forge Modular puts its Module|Patch tabs here. Returning nullptr -- which
+    /// every other product does -- adds nothing to the tree, so the home screen
+    /// is byte-identical for anything that declines.
+    virtual std::unique_ptr<pulp::view::View> home_accessory() { return nullptr; }
+
+    /// A view layered over everything, or nullptr for none.
+    ///
+    /// Mounted last, beside the publish sheet, so it paints above the composer.
+    /// Forge Modular's `@` mention list lives here: it has to sit over the
+    /// prompt card and follow the caret, which nothing in the ordinary flow can
+    /// do. Every other product returns nullptr and gets no extra view.
+    virtual std::unique_ptr<pulp::view::View> overlay_accessory() { return nullptr; }
+
+    /// An extra control in the Build title bar, or nothing.
+    ///
+    /// Mounted between the identity group and the utilities, so it reads as
+    /// part of the bar rather than a strip bolted under it. Forge's own
+    /// products return nothing and their bar is unchanged.
+    virtual std::unique_ptr<pulp::view::View> build_accessory() { return nullptr; }
+
+    /// A product-specific artifact view on the Build stage, or nothing.
+    ///
+    /// Mounted beside the materializing skeleton, hidden until the product
+    /// shows it. Forge's own products return nothing and the stage is
+    /// unchanged.
+    virtual std::unique_ptr<pulp::view::View> stage_accessory() { return nullptr; }
+
+    /// Called on the chrome's poll tick (~120ms), on the main thread.
+    ///
+    /// A product that drives work outside Forge's own generation loop has no
+    /// other way to get a heartbeat. Without it such a product can write a
+    /// perfectly good progress stream that nothing ever reads.
+    virtual void on_poll() {}
+
+    /// True while this shell has work in flight. Keeps the poll loop alive:
+    /// it otherwise stops as soon as Forge's own generation is idle, which for
+    /// an external generator is immediately.
+    virtual bool busy() const { return false; }
+
+    /// True when this shell runs its own generator and Forge's DSP+UI pipeline
+    /// must not run at all.
+    ///
+    /// Forge Modular produces VCV Rack modules and patches, which are not
+    /// audio plugins. Left to Forge's pipeline, a prompt for a Eurorack module
+    /// spends six model calls and two minutes generating a plugin -- the model
+    /// correctly protesting that "a plugin has no CV jacks" -- before failing
+    /// at an install step that could never have applied.
+    virtual bool owns_generation() const { return false; }
+
+    /// Where a prompt goes when the shell owns generation. Return a reason it
+    /// could not start, or empty.
+    virtual std::string submit_own(const std::string& prompt) {
+        (void)prompt;
+        return {};
+    }
+
+
     virtual bool has_build() const = 0;
 
     // One descriptor per bound macro of the ACTIVE build, current values.
diff --git a/src/chrome.cpp b/src/chrome.cpp
index 1c3814e..b285a84 100644
--- a/src/chrome.cpp
+++ b/src/chrome.cpp
@@ -1151,44 +1151,75 @@ private:
 // these has an answer for all three kinds — a fall-through would show effect
 // wording ("darker feedback tail", "Lo-Fi Delay") inside the instrument or
 // MIDI-effect plugin.
-const char* default_build_title(ShellKind kind) {
-    switch (kind) {
-        case ShellKind::instrument: return "Subtractive Synth";
-        case ShellKind::midi_effect: return "Note Pass";
-        case ShellKind::effect: break;
+// Product copy comes from the shell, which answers for itself. These four used
+// to switch on ShellKind here, which put product words in the layout code and
+// meant a new product could not supply its own without editing this file.
+//
+// std::string rather than const char*: the copy lives in a value the shell
+// returns, so handing back a pointer into a temporary would dangle.
+/// Build one composer action from its description.
+///
+/// The chrome owns the treatment -- size, radius, border, icon colour -- so a
+/// product describing a row cannot accidentally style itself differently from
+/// its siblings. It says what the buttons ARE; this decides how they look.
+std::unique_ptr<TextButton> make_composer_action(const ComposerAction& spec) {
+    auto b = std::make_unique<TextButton>();
+    b->set_style(spec.primary ? TextButton::Style::primary
+                              : TextButton::Style::ghost);
+    b->set_access_label(spec.access_label.empty() ? spec.label
+                                                  : spec.access_label);
+    const bool icon_only = spec.label.empty();
+    // Height only for a labelled button: size() sets a preferred WIDTH, and
+    // passing 0 collapsed the button, then its row, then the whole home column
+    // -- the render came back empty with nothing in the log to say why. A
+    // labelled button takes its width from its padding and text.
+    if (icon_only) {
+        size(*b, 34.0f, 34.0f);
+    } else {
+        b->flex().preferred_height = 34.0f;
+    }
+    if (!spec.primary) b->set_border(kLine, 1, design::radius::medium);
+    b->flex().direction = FlexDirection::row;   // default is column: labels wrapped
+    b->flex().align_items = FlexAlign::center;
+    b->flex().justify_content = FlexJustify::center;
+    if (!icon_only) {
+        b->flex().padding_left = 12;
+        b->flex().padding_right = 14;
+        b->flex().gap = 7;
     }
-    return "Lo-Fi Delay";
-}
 
-const char* prompt_placeholder(ShellKind kind) {
-    switch (kind) {
-        case ShellKind::instrument: return "a warm analog pad with a slow filter sweep…";
-        case ShellKind::midi_effect: return "a scale-locked up-down arpeggiator…";
-        case ShellKind::effect: break;
+    const Color ink = spec.primary ? kOnInk : kMuted;
+    switch (spec.icon) {
+        case ComposerAction::Icon::plus:      b->add_child(icon_plus(ink)); break;
+        case ComposerAction::Icon::dice:      b->add_child(icon_dice(ink)); break;
+        case ComposerAction::Icon::arrow_up:  b->add_child(icon_arrow_up(ink)); break;
+        case ComposerAction::Icon::document:  b->add_child(icon_document(ink)); break;
+        case ComposerAction::Icon::search:    b->add_child(icon_search(ink)); break;
+        case ComposerAction::Icon::gear:      b->add_child(icon_gear(ink)); break;
+        case ComposerAction::Icon::none:      break;
+    }
+    if (!icon_only) {
+        b->add_child(make_label(spec.label, kFontDisplay, 12.5f, 600,
+                                spec.primary ? kOnInk : kText));
     }
-    return "Build a lo-fi multi-fx plugin — reverb, delay, chorus. "
-           "Nocturne, dreamy, a visualizer per effect.";
+    if (spec.on_click) b->on_click = spec.on_click;
+    return b;
 }
 
-const char* followup_placeholder(ShellKind kind) {
-    switch (kind) {
-        case ShellKind::instrument: return "refine it — e.g. faster attack, brighter tone";
-        case ShellKind::midi_effect: return "refine it — e.g. swing the offbeats";
-        case ShellKind::effect: break;
-    }
-    return "refine it — e.g. darker feedback tail";
+std::string default_build_title(const ForgeShell& shell) {
+    return shell.chrome_copy().default_build_title;
 }
 
-std::string shell_kind_badge(ShellKind kind) {
-    switch (kind) {
-        case ShellKind::instrument:
-            return std::string(brand::kProductNameUpper) + " INST";
-        case ShellKind::midi_effect:
-            return std::string(brand::kProductNameUpper) + " MIDI";
-        case ShellKind::effect:
-            return std::string(brand::kProductNameUpper) + " FX";
-    }
-    return std::string(brand::kProductNameUpper);
+std::string prompt_placeholder(const ForgeShell& shell) {
+    return shell.chrome_copy().prompt_placeholder;
+}
+
+std::string followup_placeholder(const ForgeShell& shell) {
+    return shell.chrome_copy().followup_placeholder;
+}
+
+std::string shell_kind_badge(const ForgeShell& shell) {
+    return shell.chrome_copy().badge;
 }
 
 std::string trim_copy(const std::string& s) {
@@ -1350,7 +1381,7 @@ ForgeChrome::ForgeChrome(ForgeShell& shell, gen::GenerationSession& session)
     // shell ships a lo-fi delay; the instrument shell a subtractive synth; the
     // MIDI-effect shell a pass-through transform chain). A generated build
     // overrides this with its own name at install.
-    project_title_ = default_build_title(shell_.kind());
+    project_title_ = default_build_title(shell_);
 }
 
 ForgeChrome::~ForgeChrome() {
@@ -1476,6 +1507,14 @@ std::unique_ptr<pulp::view::View> ForgeChrome::build() {
     settings_surface_->build(*root);  // above the confirm sheet
     audio_assets_surface_->build(*root);  // capability-aware asset authoring
     build_publish_sheet(*root);   // marketplace authoring is the topmost sheet
+
+    // A product may layer one more thing above everything -- Forge Modular's
+    // mention list. Added after the sheet so it paints last, and absent for
+    // anything that returns nullptr.
+    if (auto overlay = shell_.overlay_accessory()) {
+        overlay_accessory_ = overlay.get();
+        root->add_child(std::move(overlay));
+    }
     restore_open_state();
     // A terminal turn may predate this editor instance. Its transaction state
     // is shell-owned, so rematerialize the actions just as we rematerialize the
@@ -1683,7 +1722,7 @@ void ForgeChrome::build_title_bar(View& parent) {
     player_title_ = title.get();
     title->flex().flex_shrink = 0;
     identity->add_child(std::move(title));
-    auto tag = make_label(shell_kind_badge(shell_.kind()), kFontMono, 10, 500,
+    auto tag = make_label(shell_kind_badge(shell_), kFontMono, 10, 500,
                           kFaint, 0.8f);
     shell_kind_badge_ = tag.get();
     tag->set_border(kLine, 1, 5);
@@ -1766,6 +1805,12 @@ void ForgeChrome::build_title_bar(View& parent) {
     size(*init, 52, 24);
     init->on_click = [this]() { save_player_state(); };
     utilities->add_child(std::move(init));
+    // Between identity and utilities: a product-specific control belongs in
+    // the bar, not on a strip bolted underneath it.
+    if (auto accessory = shell_.build_accessory()) {
+        build_accessory_ = accessory.get();
+        bar->add_child(std::move(accessory));
+    }
     bar->add_child(std::move(utilities));
 
     parent.add_child(std::move(bar));
@@ -1881,8 +1926,7 @@ void ForgeChrome::build_home(View& parent) {
 
     hero->add_child(make_spacer());
 
-    auto eyebrow = make_label(std::string(brand::kProductNameUpper) +
-                                  " · AI PLUGIN FACTORY",
+    auto eyebrow = make_label(shell_.chrome_copy().hero_eyebrow,
                               kFontMono, 11, 400,
                               kMuted, 1.76f);
     home_eyebrow_ = eyebrow.get();
@@ -1890,13 +1934,27 @@ void ForgeChrome::build_home(View& parent) {
     eyebrow->flex().flex_shrink = 0;
     hero->add_child(std::move(eyebrow));
 
-    auto title = make_label("What do you want to build?", kFontDisplay,
+    auto title = make_label(shell_.chrome_copy().hero_title, kFontDisplay,
                             kHeroTitleSize, 700, kTextStrong, -0.84f);
     home_title_ = title.get();
     title->flex().margin_bottom = 30;
     title->flex().flex_shrink = 0;
     hero->add_child(std::move(title));
 
+    // A product may put something between the title and the composer -- Forge
+    // Modular's Module|Patch tabs live here. Every other product returns
+    // nullptr, which adds nothing to the tree, so their home screens stay
+    // byte-identical.
+    if (auto accessory = shell_.home_accessory()) {
+        accessory->flex().flex_shrink = 0;
+        // Centred and content-sized. The hero column stretches its children, so
+        // without this the accessory spanned the full width and pushed its
+        // contents to opposite edges.
+        accessory->flex().align_self = FlexAlign::center;
+        accessory->flex().margin_bottom = 14;
+        hero->add_child(std::move(accessory));
+    }
+
     // Prompt card: a writing row above the composer toolbar. The card has no
     // authored height — it is exactly as tall as the prompt, the attachment
     // strip, and the toolbar need, so it opens compact and grows around the
@@ -1946,7 +2004,7 @@ void ForgeChrome::build_home(View& parent) {
         kHomePromptTextWidth, design::geometry::composer_max_text_lines,
         design::geometry::composer_text_line_height);
     prompt_input_ = input.get();
-    input->placeholder = prompt_placeholder(shell_.kind());
+    input->placeholder = prompt_placeholder(shell_);
     input->set_font_family(kFontDisplay);
     // The HTML is Jost 16/24 in a 642 px content box. Pulp's native Jost
     // shaping is fractionally wider, so a half-point optical compensation
@@ -1993,6 +2051,28 @@ void ForgeChrome::build_home(View& parent) {
     actions->flex().align_items = FlexAlign::center;
     actions->flex().margin_top = 10;
     actions->flex().flex_shrink = 0;
+
+    // A product may describe its own row. An empty description means "the
+    // standard one", so the three original products are unaffected and this is
+    // not a breaking change for anything that does not care.
+    if (auto described = shell_.composer_row();
+        !described.left.empty() || !described.right.empty()) {
+        for (const auto& a : described.left) {
+            auto b = make_composer_action(a);
+            b->flex().margin_right = 8;
+            actions->add_child(std::move(b));
+        }
+        actions->add_child(make_spacer());
+        for (const auto& a : described.right) {
+            auto b = make_composer_action(a);
+            b->flex().margin_left = 8;
+            actions->add_child(std::move(b));
+        }
+        box->add_child(std::move(actions));
+        actions = nullptr;   // the standard row below is skipped, not the rest
+    }
+
+    if (actions) {
     auto attach = std::make_unique<TextButton>();
     home_attach_button_ = attach.get();
     attach->set_style(TextButton::Style::ghost);
@@ -2047,6 +2127,7 @@ void ForgeChrome::build_home(View& parent) {
     };
     actions->add_child(std::move(send));
     box->add_child(std::move(actions));
+    }
 
     // The composer lives in a fixed-height slot and is centred in it, so the
     // hero's eyebrow and headline hold their authored positions while the card
@@ -3401,7 +3482,7 @@ void ForgeChrome::build_chat_rail(View& parent) {
     follow->add_child(std::move(attachment_error));
     auto input = std::make_unique<TextEditor>();
     followup_input_ = input.get();
-    input->placeholder = followup_placeholder(shell_.kind());
+    input->placeholder = followup_placeholder(shell_);
     input->set_font_family(kFontDisplay);
     input->set_font_size(14);
     input->set_background_color(kRaised);
@@ -3611,6 +3692,13 @@ void ForgeChrome::build_stage(View& parent) {
     scripted->set_visible(false);
     stage->add_child(std::move(scripted));
 
+    if (auto accessory = shell_.stage_accessory()) {
+        stage_accessory_ = accessory.get();
+        // The skeleton owns the stage until an artifact exists.
+        accessory->set_visible(false);
+        stage->add_child(std::move(accessory));
+    }
+
     viewport->add_child(std::move(stage));
     viewport->set_content_size(
         {static_cast<float>(kDesignWidth), static_cast<float>(kDesignHeight)});
@@ -4268,7 +4356,7 @@ void ForgeChrome::apply_build_view() {
     if (followup_input_) {
         followup_input_->placeholder =
             code ? "Describe an edit to the live source"
-                 : followup_placeholder(shell_.kind());
+                 : followup_placeholder(shell_);
     }
     if (designer_pane_) designer_pane_->set_visible(design);
     if (code_tab_button_) {
@@ -4327,6 +4415,16 @@ void ForgeChrome::enter_home() {
     apply_modes();
 }
 
+void ForgeChrome::set_project_title_from_prompt(const std::string& prompt) {
+    if (prompt.empty()) return;
+    project_title_ = gen::display_title_from_prompt(prompt);
+    // refresh_shell_chrome(), not refresh_copy(): the title bar's label is
+    // painted there, and refreshing only the copy left the heading reading
+    // "Untitled module" for the whole build.
+    refresh_shell_chrome();
+    if (root_) root_->request_repaint();
+}
+
 void ForgeChrome::enter_build() {
     // The shell's generated source is worker-owned until the exact terminal
     // event has been reduced. Build can become the visible destination now,
@@ -5726,7 +5824,7 @@ void ForgeChrome::on_project_restored() {
         // An empty host payload is a real reset to the hand-fed default build,
         // not merely a source change. Drop the departed project's identity and
         // conversation before rematerializing the default DSP.
-        project_title_ = default_build_title(shell_.kind());
+        project_title_ = default_build_title(shell_);
         clear_chat_rail();
     }
     refresh_shelf();
@@ -5859,7 +5957,7 @@ void ForgeChrome::confirm_delete_project() {
     // The deleted default tile stays gone; saved entries keep showing.
     default_tile_alive_ = false;
     // Back to this kind's default build's name, for a fresh start.
-    project_title_ = default_build_title(shell_.kind());
+    project_title_ = default_build_title(shell_);
     enter_home();
 }
 
@@ -5920,6 +6018,16 @@ bool ForgeChrome::submit_prompt_impl(const std::string& prompt,
     // reduced, so it owns the mutation transaction for that whole interval.
     if (marketplace_open_worker_.joinable()) return false;
     const std::string text = trim_copy(prompt);
+
+    // A shell with its own generator takes the prompt here and Forge's
+    // DSP+UI pipeline never starts. Checked before any project or transcript
+    // bookkeeping, so a Rack prompt cannot leave half a plugin build behind.
+    if (shell_.owns_generation()) {
+        if (text.empty()) return false;
+        const auto why = shell_.submit_own(text);
+        if (!why.empty()) append_agent_line(why, /*error=*/true);
+        return why.empty();
+    }
     std::optional<RecoverableTurn> pending_replay;
     bool replaying_pending_submission = false;
     const RecoverableTurn* pending_submission =
@@ -6189,6 +6297,10 @@ bool ForgeChrome::submit_prompt_impl(const std::string& prompt,
 }
 
 void ForgeChrome::poll() {
+    // A shell driving work outside Forge's generation loop needs the same
+    // heartbeat. Inside poll() rather than beside one caller, so it arrives on
+    // both drivers -- the dispatcher fallback and a host-installed FrameClock.
+    shell_.on_poll();
     // Publication callbacks may arrive on arbitrary provider threads. They
     // write only to a shared mailbox; the host's UI-thread tick owns every
     // ForgeChrome and widget mutation.
@@ -6379,7 +6491,7 @@ void ForgeChrome::schedule_poll() {
             if (!alive->load(std::memory_order_acquire)) return;
             poll_armed_ = false;
             poll();
-            if (generating() || session_.running() ||
+            if (generating() || session_.running() || shell_.busy() ||
                 !pending_open_entry_id_.empty() ||
                 code_autosave_pending_) {
                 schedule_poll();
@@ -8630,6 +8742,19 @@ void ForgeChrome::refresh_marketplace_category_styles() {
     }
 }
 
+void ForgeChrome::refresh_copy() {
+    const auto copy = shell_.chrome_copy();
+    if (home_eyebrow_) home_eyebrow_->set_text(copy.hero_eyebrow);
+    if (home_title_) home_title_->set_text(copy.hero_title);
+    if (shell_kind_badge_) shell_kind_badge_->set_text(copy.badge);
+    // The placeholder is only a suggestion, so it is replaced only while the
+    // field is empty -- overwriting something half-typed would be worse than
+    // leaving a stale hint.
+    if (prompt_input_ && prompt_input_->text().empty())
+        prompt_input_->placeholder = copy.prompt_placeholder;
+    if (root_) root_->request_repaint();
+}
+
 void ForgeChrome::rebuild_marketplace_cards(
     const std::vector<const MarketplaceListing*>& listings,
     bool reference_catalog) {
@@ -9019,7 +9144,7 @@ void ForgeChrome::refresh_shelf() {
                              : Color::rgba8(0x10, 0x16, 0x2E)},
             {0.0f, 1.0f});
         auto format = make_label(
-            shell_kind_badge(shell_.kind()),
+            shell_kind_badge(shell_),
             kFontMono, 9, 500, Color::rgba8(0xFF, 0xFF, 0xFF, 0xB3), 0.9f);
         art->add_child(std::move(format));
         tile->add_child(std::move(art));
