#pragma once

// Deprecated include path. The types moved to <pulp/audio/waveform_overview.hpp>
// and are now spelled `WaveformOverview` / `WaveformOverviewCache`; deprecated
// aliases for the old spellings live there too, so code that included this header
// keeps compiling unchanged. New code should include the new path directly.

#include <pulp/audio/waveform_overview.hpp>
