#pragma once

// Deprecated include path. The type moved to <pulp/runtime/spsc_ring_index.hpp>
// and is now spelled `pulp::runtime::SpscRingIndex`; a deprecated alias for the
// old spelling lives there too, so code that included this header keeps
// compiling unchanged. New code should include the new path directly.

#include <pulp/runtime/spsc_ring_index.hpp>
