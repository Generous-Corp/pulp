# Local CI Module Map

This map is the Phase 1.3 contract for `tools/local-ci`: it records the
current ownership boundaries before more code moves out of `local_ci.py`.
Future extraction PRs should preserve the contracts below or update this file
and the matching contract tests in the same change.

## Extracted Modules

| Module | Owns | Must not own |
| --- | --- | --- |
| `android_target.py` | Android SDK/NDK discovery, Android Gradle prerequisite checks, and debug APK build invocation metadata. | Queue orchestration, desktop automation, GitHub Actions dispatch, or cross-target validation execution. |
| `binding_utils.py` | Shared lookup helper for local_ci facade binding modules. | Facade export policy, target behavior, command dispatch, or queue mutation. |
| `module_test_utils.py` | Shared dynamic module loader for local-ci facade binding tests. | Production local-CI behavior, facade export policy, target behavior, command dispatch, or queue mutation. |
| `state_paths.py` | State/config/log/bundle path resolution and state-directory creation. | Queue JSON contents, result schema, or target behavior. |
| `state_path_bindings.py` | Dependency binding between the `local_ci.py` facade and state path helpers, preserving facade-level monkeypatch seams while moving path delegation out of the entrypoint. | State path behavior, queue JSON contents, result schema, or target behavior. |
| `cli_parser.py` | Argument parser construction for top-level, cloud, cleanup, evidence, and desktop subcommands. | Command handler dispatch, command execution, or user-facing output side effects. |
| `cli_parser_bindings.py` | Dependency binding between the `local_ci.py` facade and parser construction, preserving facade-level parser constants, direct parser construction, and epilog wiring while moving parser dependency assembly out of the entrypoint. | Parser behavior, command dispatch, command execution, or output formatting. |
| `cli_dispatch.py` | Parsed command dispatch for top-level, cloud, cloud namespace, and desktop subcommands. | Argument parser construction, command handler side effects, or command output formatting. |
| `cli_dispatch_bindings.py` | Dependency binding between the `local_ci.py` facade and CLI dispatch helpers, preserving facade-level monkeypatch seams while moving command-handler maps out of the entrypoint. | Argument parser construction, command handler side effects, or command output formatting. |
| `cloud_bindings.py` | Dependency binding between the `local_ci.py` facade and cloud/GitHub helpers, preserving facade-level command, GitHub API, cloud record, billing/cost, provider metadata, Namespace, and PR formatting monkeypatch seams while moving default helper delegation out of the entrypoint. | Cloud command behavior, GitHub/Namespace subprocess policy, cloud record persistence, billing/cost policy, or workflow/provider selection. |
| `config_evidence_bindings.py` | Dependency binding between the `local_ci.py` facade and config/evidence helpers, preserving facade-level monkeypatch seams while moving config load/save and evidence summary wiring out of the entrypoint. | Config path policy, evidence index persistence behavior, queue mutation, target execution, or command output orchestration. |
| `io_utils.py` | Atomic writes, file locks, log tailing, line trimming, artifact path polling, and image-change summaries. | CI state semantics or target orchestration. |
| `io_utils_bindings.py` | Dependency binding between the `local_ci.py` facade and I/O helpers, preserving facade-level monkeypatch seams while moving file/lock helper delegation out of the entrypoint. | I/O behavior, lock policy, CI state semantics, or target orchestration. |
| `normalize.py` | Priority, validation mode, desktop mode, boolean, adapter, and desktop config normalization. | Queue persistence or dispatch side effects. |
| `normalize_bindings.py` | Dependency binding between the `local_ci.py` facade and normalization helpers/constants, preserving facade-level monkeypatch seams while moving normalization delegation out of the entrypoint. | Normalization behavior, queue persistence, or dispatch side effects. |
| `provenance.py` | Stable direct/hosted provenance dictionaries and summaries for jobs and results. | GitHub or Shipyard API calls. |
| `provenance_bindings.py` | Dependency binding between the `local_ci.py` facade and provenance helpers, preserving facade-level monkeypatch seams while moving provenance delegation out of the entrypoint. | Provenance behavior, GitHub or Shipyard API calls, queue persistence, or result persistence. |
| `git_helpers.py` | Git/time helper seam: current branch/SHA/ref resolution, git-root discovery, short SHA formatting, GitHub remote URL normalization, origin URL lookup, and generic git subprocess execution. | Local-CI queue state, target execution, desktop report publishing policy, or GitHub API calls. |
| `git_helpers_bindings.py` | Dependency binding between the `local_ci.py` facade and git/time helpers, preserving facade-level monkeypatch seams while moving branch/SHA/ref helper delegation out of the entrypoint. | Git helper behavior, local-CI queue state, target execution, desktop report publishing policy, or GitHub API calls. |
| `job_queue.py` | Queue entry normalization and unlocked queue load/save. | Lock acquisition, stale-running reconciliation, supersedence side effects. |
| `job_queue_bindings.py` | Dependency binding between the `local_ci.py` facade and job queue helpers, preserving facade-level monkeypatch seams while moving queue persistence delegation out of the entrypoint. | Queue persistence behavior, lock acquisition, stale-running reconciliation, or supersedence side effects. |
| `targets.py` | Enabled-target discovery, `--targets` parsing, and configured target resolution. | Transport-specific preflight, SSH probes, fallback routing. |
| `target_bindings.py` | Dependency binding between the `local_ci.py` facade and target selection helpers, preserving facade-level monkeypatch seams while moving target resolution delegation out of the entrypoint. | Target selection behavior, transport-specific preflight, SSH probes, or fallback routing. |
| `github_workflows.py` | Pure GitHub Actions workflow/default/provider resolution. | `gh` subprocess calls, workflow dispatch, or polling. |
| `github_workflow_bindings.py` | Dependency binding between the `local_ci.py` facade and GitHub workflow/default/provider constants and resolvers, preserving facade-level monkeypatch seams while moving resolver delegation out of the entrypoint. | Workflow/default/provider resolution behavior, `gh` subprocess calls, workflow dispatch, or polling. |
| `cloud.py` | GitHub/Namespace cloud-run records, cost/history helpers, dispatch wrappers, PR-list line fragments, and formatting. | Local/SSH validation execution. |
| `footprint.py` | Local-CI state-size accounting, cleanup entry descriptions, and state-footprint line fragments. | Cleanup candidate selection or deletion. |
| `footprint_bindings.py` | Dependency binding between the `local_ci.py` facade and footprint helpers, preserving facade-level monkeypatch seams while moving state-size helper delegation out of the entrypoint. | Footprint behavior, cleanup candidate selection, or deletion. |
| `evidence_index.py` | Result-to-evidence normalization, latest passing target evidence, evidence index persistence, evidence summaries, and evidence-command line fragments. | Queue mutation, runner state, result creation, or target execution. |
| `evidence_index_bindings.py` | Dependency binding between the `local_ci.py` facade and evidence-index helpers, preserving facade-level monkeypatch seams while moving evidence-index helper delegation out of the entrypoint. | Evidence-index behavior, queue mutation, runner state, result creation, or target execution. |
| `ssh_subprocess.py` | SSH subprocess retry policy and transient-failure classification. | Target preflight policy, Windows probe semantics, queue mutation, or target validation execution. |
| `ssh_subprocess_bindings.py` | Dependency binding between the `local_ci.py` facade and SSH subprocess retry helpers, preserving facade-level subprocess/sleep monkeypatch seams. | SSH retry policy, target preflight policy, Windows probe semantics, queue mutation, or target validation execution. |
| `ssh_bundle.py` | Git bundle naming, local bundle creation, and SSH upload/progress/probe mechanics. | Target validation execution or queue orchestration. |
| `ssh_bundle_bindings.py` | Dependency binding between the `local_ci.py` facade and SSH bundle helpers, preserving facade-level monkeypatch seams while moving bundle build/upload and uploaded-size probe wiring out of the entrypoint. | Bundle upload policy, SSH transport implementation, target validation execution, or queue orchestration. |
| `cleanup.py` | Local-CI artifact cleanup planning/deletion, cleanup-plan line fragments, and stale Windows validator cleanup mechanics. | Lock acquisition, runner ownership, or command orchestration side effects. |
| `cleanup_bindings.py` | Dependency binding between the `local_ci.py` facade and cleanup helpers, preserving facade-level monkeypatch seams while moving cleanup plan and stale-validator cleanup wiring out of the entrypoint. | Cleanup policy, queue mutation, runner ownership, or command orchestration side effects. |
| `cleanup_cli.py` | Cleanup command orchestration: running-job apply guard, cleanup-plan/footprint print ordering, deletion result rendering, and prepared-cache consequence notes. | Cleanup candidate selection/deletion, state-size accounting, queue mutation, runner ownership, or other command handlers. |
| `logs_cli.py` | Logs command orchestration: active/latest job selection wrapper, target/all-log path selection, saved-log tail output ordering, and missing-log error rendering. | Queue mutation, runner-info persistence, log file writing, target execution, or other command handlers. |
| `evidence_cli.py` | Evidence command orchestration: current-branch fallback, evidence-scope header output, summary exit-code handling, and empty-result rendering. | Evidence index persistence, evidence grouping/formatting, queue mutation, or other command handlers. |
| `queue_commands_cli.py` | Queue command CLI orchestration for `bump` and `cancel`: priority normalization error handling, locked queue-command call dispatch, result-line printing, and exit-code mapping. | Queue lock acquisition/mutation, queue policy, result persistence, runner-info persistence, or target execution. |
| `notifications.py` | Terminal bell and macOS notification side effects for completed local-CI commands. | Command result policy, queue mutation, runner state, or validation execution. |
| `notification_bindings.py` | Dependency binding between the `local_ci.py` facade and notification helpers, preserving facade-level print/subprocess monkeypatch seams while moving notification side-effect wiring out of the entrypoint. | Notification behavior, command result policy, queue mutation, runner state, or validation execution. |
| `utility_command_bindings.py` | Dependency binding between the `local_ci.py` facade and cleanup, queue-command, logs, and evidence CLI helpers, preserving facade-level monkeypatch seams while moving utility command wiring out of the entrypoint. | Command behavior, queue mutation, cleanup policy, log/evidence formatting, or target execution. |
| `local_ci_command_bindings.py` | Dependency binding between the `local_ci.py` facade and top-level local-CI command helpers, preserving facade-level monkeypatch seams while moving enqueue/drain/run/ship/check/list/status wiring out of the entrypoint. | Command behavior, parser construction, desktop subcommands, cloud subcommands, queue policy, target execution, runner-info persistence, or artifact/report internals. |
| `local_ci_commands_cli.py` | Top-level local-CI command orchestration for enqueue, drain, run, ship, check, list, status, and shared submission option resolution. | Parser construction, desktop subcommands, cloud subcommands, queue policy, target execution, runner-info persistence, or artifact/report internals. |
| `desktop_actions.py` | Desktop automation action helper policy: action artifact path layout, interaction detection/summaries, coordinate parsing, view-tree click selection, inspector summaries, content-size mapping, screen-point mapping, default labels, and view-tree counts. | Target execution, artifact persistence, report rollups, or OS-specific launch/probe helpers. |
| `desktop_command_bindings.py` | Dependency binding between the `local_ci.py` facade and desktop setup, management, and action command helpers, preserving facade-level monkeypatch seams while moving desktop command wiring out of the entrypoint. | Desktop command behavior, launch/action execution, artifact/report internals, parser construction, or desktop line fragment formatting. |
| `desktop_action_commands_cli.py` | Desktop automation action command orchestration for smoke/click/inspect: config/target/source loading, adapter selection, command-specific validation, JSON/text result dispatch, and runner callable invocation. | Platform launch implementations, artifact internals, report rollups, parser construction, or desktop line fragment formatting. |
| `desktop_commands_cli.py` | Desktop automation management command orchestration for status/config/recent/proof/publish/cleanup: config load/save error handling, payload assembly, JSON/text output dispatch, report staging dispatch, and cleanup deletion/rollup ordering. | Desktop launch/action execution, artifact/report internals, line fragment formatting, target probes, or desktop action policy. |
| `desktop_setup_commands_cli.py` | Desktop automation setup and health command orchestration for install/doctor: target/config loading, artifact-root writability gating, receipt payload assembly, Windows setup dependency dispatch, repo-path persistence dispatch, and doctor JSON/text result rendering. | Platform probe/bootstrap implementations, SSH/PowerShell mechanics, artifact internals, action execution, parser construction, or doctor check derivation. |
| `desktop_cli.py` | Desktop automation CLI line fragments for status, config, action success, recent-run, proof, publish, and cleanup output. | Target execution, artifact persistence, report rollups, proof selection, or desktop action policy. |
| `desktop_artifacts.py` | Desktop automation receipt lookup plus artifact roots and run/publish bundle directory layout. | Report staging, rollup generation, pruning policy, or target execution. |
| `desktop_doctor.py` | Desktop automation capability derivation, writable-artifact checks, WebDriver status probing, and desktop doctor check assembly. | CLI output formatting, desktop action execution, artifact/report persistence, or launch adapter orchestration. |
| `desktop_infra_bindings.py` | Dependency binding between the `local_ci.py` facade and generic git, directory, slug, and wait helpers used by desktop source/reporting flows, preserving monkeypatch seams while moving infrastructure wiring out of the entrypoint. | Git/reporting helper behavior, desktop action execution, source-prep policy, or report rollup semantics. |
| `desktop_support_bindings.py` | Dependency binding between the `local_ci.py` facade and generic desktop artifact, target selection, doctor, and action helpers, preserving facade-level monkeypatch seams while moving generic desktop support wiring out of the entrypoint. | Desktop helper behavior, OS-specific launch/action adapters, reporting internals, or desktop command wiring. |
| `desktop_probe_bindings.py` | Dependency binding between the `local_ci.py` facade and desktop doctor / Windows probe helpers, preserving facade-level monkeypatch seams while moving probe wiring out of the entrypoint. | Probe policy, remote transport implementation, desktop command behavior, artifact/report persistence, or target execution. |
| `reporting.py` | Desktop automation report staging, branch-publish directory helpers, run/publish rollups, manifest scanning, proof summaries, and pruning selection. | Artifact directory layout, target execution, source preparation, or CLI output. |
| `desktop_reporting_bindings.py` | Dependency binding between the `local_ci.py` facade and desktop reporting helpers, preserving facade-level monkeypatch seams while moving report/rollup/prune wiring out of the entrypoint. | Report policy, artifact layout, git publishing implementation, desktop command behavior, or target execution. |
| `source_prep.py` | Exact-SHA desktop source requests, cache keys, launch-command rewriting, prepare manifests, and macOS/Linux/Windows prepared source materialization. | Desktop artifact layout, target execution, queue orchestration, or target probes. |
| `source_prep_bindings.py` | Dependency binding between the `local_ci.py` facade and desktop source-preparation helpers, preserving facade-level monkeypatch seams while moving source request, command rewrite, manifest attachment, and exact-SHA source wiring out of the entrypoint. | Source-prep policy, remote transport implementation, desktop artifact layout, or queue orchestration. |
| `target_preflight.py` | SSH primary/fallback/UTM/Namespace failover checks, material config drift detection, and submission metadata formatting. | Queue mutation, target validation execution, or desktop automation adapters. |
| `target_preflight_bindings.py` | Dependency binding between the `local_ci.py` facade and target preflight helpers, preserving facade-level monkeypatch seams while moving SSH/UTM/config/submission preflight wiring out of the entrypoint. | Target preflight policy, queue mutation, target validation execution, or desktop automation adapters. |
| `queue_bindings.py` | Dependency binding between the `local_ci.py` facade and queue lifecycle/runner-state helpers, preserving facade-level monkeypatch seams while moving queue policy/display delegation, locked queue load/reconcile/save wiring, stale-running reconciliation, target-state update, runner-info, and stale-validator reclaim wiring out of the entrypoint. | Queue policy, cleanup helper wiring, result policy, target execution, or CLI output formatting. |
| `queue_orchestrator.py` | Queue policy and in-memory queue helpers: branch validation, default priorities, job fingerprints/records, enqueue duplicate lookup, pending priority bumps, enqueue supersedence candidate selection, queue-command lookup, priority mutation, enqueue/queue-command result line fragments, drain active-runner line fragments, supersedence and cancellation result payloads, terminal and completed job-state mutation, stale-running job selection/replacement/requeue state, stale-running reconciliation action selection, job and target-state summaries, status grouping, status active-target and target-state selection, initial/progress/completed target-state payload construction, target-state snapshots, status submission/target line fragments, runner status line fragments, result-display and log-command line fragments, recent-completed status selection and result summaries, retention trimming, priority sort keys, job lookup/log selection, active-target upserts, target-state updates, job claiming, and runner-info active-target mutation. | Queue file locking, runner-info persistence, runner liveness, result persistence, drain orchestration, target execution, or user-facing CLI section ordering/output. |
| `queue_lifecycle.py` | Locked queue enqueue/load/claim/finalize lifecycle helpers, active-target/target-state update persistence, superseded/canceled completion result persistence, CLI queue bump/cancel persistence, stale-validator queue collection, running-job reconciliation action application, drain loop coordination, and job-wait polling that coordinate queue lock acquisition, running-job reconciliation, queue persistence, runner claim records, completion trimming, post-finalize cleanup, and wait-loop callbacks through injected dependencies. | Enqueue/drain policy, runner-info persistence, result payload policy, target execution, or user-facing CLI output. |
| `runner_state.py` | Runner-info JSON persistence, runner PID liveness checks, stale runner-info cleanup under the drain lock, current-runner stale-running selection, and current-runner active-target write orchestration. | Queue mutation, target execution, runner active-target payload policy, or user-facing CLI output. |
| `linux_target.py` | Linux desktop launch backend probes, remote tooling probes/readiness details, remote bundle paths, and remote command builders. | SSH execution orchestration, artifact fetch/cleanup, queue orchestration, or manifest writing. |
| `linux_target_bindings.py` | Dependency binding between the `local_ci.py` facade and Linux target helpers, preserving facade-level monkeypatch seams while moving launch/tooling probe, remote bundle path, and remote command-builder wiring out of the entrypoint. | Linux target policy, SSH execution orchestration, artifact fetch/cleanup, queue orchestration, or manifest writing. |
| `linux_desktop_bindings.py` | Dependency binding between the `local_ci.py` facade and Linux desktop action execution helpers, preserving facade-level monkeypatch seams while moving action and SSH artifact transfer wiring out of the entrypoint. | Linux probe/command-builder policy, manifest/action policy, source preparation, or CLI command parsing. |
| `linux_desktop_action.py` | Linux desktop action execution: remote command dispatch, SSH artifact fetch/cleanup helpers, run manifest assembly, source-context attachment, interaction summaries, and run rollup writes. | Linux probe/command-builder policy, desktop command parsing, queue orchestration, or macOS/Windows action execution. |
| `macos_desktop.py` | macOS app bundle detection, Swift window-probe wrappers, window wait/capture/activation/click helpers, app quit, and process termination. | Desktop action orchestration, artifact manifest writing, source preparation, or queue orchestration. |
| `macos_window_bindings.py` | Dependency binding between the `local_ci.py` facade and macOS window helpers, preserving facade-level monkeypatch seams while moving app-bundle, window probe, activation, capture, click, quit, and process termination wiring out of the entrypoint. | macOS primitive policy, desktop action orchestration, artifact manifest writing, source preparation, or queue orchestration. |
| `macos_desktop_bindings.py` | Dependency binding between the `local_ci.py` facade and macOS desktop action execution helpers, preserving facade-level monkeypatch seams while moving action wiring out of the entrypoint. | macOS primitive implementations, manifest/action policy, source preparation, or CLI command parsing. |
| `macos_desktop_action.py` | macOS desktop action execution: command/app launch, UI snapshot waits, desktop-event and Pulp-app interactions, screenshot capture, manifest assembly, source-context attachment, and run rollup writes. | macOS window/capture primitive implementations, desktop command parsing, queue orchestration, or Linux/Windows action execution. |
| `windows_desktop_bindings.py` | Dependency binding between the `local_ci.py` facade and Windows desktop action execution helpers, preserving facade-level monkeypatch seams while moving action wiring out of the entrypoint. | Windows target contract/probe policy, manifest/action policy, source preparation, or CLI command parsing. |
| `windows_desktop_action.py` | Windows desktop action execution: session-agent request dispatch, remote artifact fetch/cleanup, run manifest assembly, source-context attachment, interaction summaries, and run rollup writes. | Windows target contract/probe policy, desktop command parsing, queue orchestration, or macOS/Linux action execution. |
| `windows_target.py` | Windows desktop target contracts, path safety, session-agent request payloads, and probe result formatting/readiness helpers. | SSH/PowerShell execution, desktop action execution, queue orchestration, or artifact layout. |
| `windows_target_bindings.py` | Dependency binding between the `local_ci.py` facade and Windows target helpers, preserving facade-level monkeypatch seams while moving session-task, path-safety, repo/tooling detail, and session-agent request wiring out of the entrypoint. | Windows target policy, SSH/PowerShell execution, desktop action execution, or artifact layout. |
| `windows_probe_bindings.py` | Dependency binding between the `local_ci.py` facade and Windows SSH/PowerShell probe helpers, preserving facade-level monkeypatch seams while moving remote transport, file transfer, session-agent, and CMake probe wiring out of the entrypoint. | Windows probe behavior, target contract formatting, desktop action orchestration, queue orchestration, or artifact layout. |
| `windows_probe.py` | Windows SSH/PowerShell command execution helpers, remote file transfer/read/remove helpers, repo/session/tooling probes, remote tool installation, session-agent bootstrap/start, and CMake generator probing. | Windows target contract formatting, desktop action orchestration, queue orchestration, or artifact layout. |
| `execution_bindings.py` | Dependency binding between the `local_ci.py` facade and validation execution helpers, preserving facade-level monkeypatch seams while moving execution helper delegation, runner, command/result construction, and job-processing wiring out of the entrypoint. | Validation command construction, result policy, queue orchestration, or platform probe implementations. |
| `windows_validation_script.py` | Windows PowerShell validation script construction: host mutex acquisition, bundle fetch, prepared-state reuse/cleanup, CMake configure/build/install/smoke/test phases, and progress marker emission. | Windows checkout/probe helpers, PowerShell transport execution, queue mutation, result policy, or desktop automation adapters. |
| `validation_commands.py` | Target validation command construction, prepared-state reuse policy, missing-commit error text, and Windows validation-script dependency assembly. | Subprocess execution, target task orchestration, queue mutation, runner-info persistence, Windows checkout/probe helper internals, or desktop automation adapters. |
| `execution.py` | Subprocess output capture, progress marker parsing, heartbeat updates, optional command log writing, local/POSIX/Windows runner orchestration, job/target result construction and ordering, result file persistence/rendering, target task construction/execution/result collection, submission config resolution, SSH target execution planning, cross-target validation/status orchestration, and target-neutral validation helper policy. | Validation command construction, Windows validation script text, Windows checkout/probe helper internals, queue mutation, runner-info persistence, or desktop automation adapters. |

## `local_ci.py` Facade Contract

`local_ci.py` is now a compatibility entry point for tests, scripts, and callers
that import helpers directly. Its remaining responsibility is import-time
dependency assembly: it imports focused implementation modules, lets the matching
`*_bindings.py` modules install facade-level exports, preserves a small set of
private aliases required by existing tests/callers, and keeps `main()` as the
CLI entrypoint.

Future extraction PRs should not add new behavior to `local_ci.py`. Move behavior
into a focused implementation module, add or update the matching binding module,
install the facade export there, and update the module ownership row above. The
facade-binding contract is pinned by `test_local_ci_facade_bindings.py`:

- public facade functions must reference a binding module;
- public facade constants must delegate through binding modules;
- private implementation imports must be consumed only by binding modules.

The installed exports currently cover these ownership groups:

| Group | Binding modules | Implementation modules |
| --- | --- | --- |
| Desktop action orchestration | `desktop_support_bindings.py`, `desktop_infra_bindings.py`, `desktop_reporting_bindings.py`, `desktop_probe_bindings.py`, `desktop_command_bindings.py`, `macos_window_bindings.py`, `macos_desktop_bindings.py`, `linux_desktop_bindings.py`, `windows_desktop_bindings.py` | `desktop_actions.py`, `desktop_artifacts.py`, `desktop_doctor.py`, `desktop_cli.py`, `desktop_commands_cli.py`, `desktop_setup_commands_cli.py`, `desktop_action_commands_cli.py`, `reporting.py`, `source_prep.py`, `macos_desktop.py`, `macos_desktop_action.py`, `linux_desktop_action.py`, `windows_desktop_action.py` |
| Queue orchestration | `job_queue_bindings.py`, `queue_bindings.py`, `cleanup_bindings.py`, `utility_command_bindings.py`, `local_ci_command_bindings.py` | `job_queue.py`, `queue_lifecycle.py`, `queue_orchestrator.py`, `runner_state.py`, `cleanup.py`, `cleanup_cli.py`, `logs_cli.py`, `evidence_cli.py`, `queue_commands_cli.py`, `local_ci_commands_cli.py` |
| Validation execution | `execution_bindings.py`, `target_bindings.py`, `target_preflight_bindings.py`, `ssh_subprocess_bindings.py`, `ssh_bundle_bindings.py`, `linux_target_bindings.py`, `windows_target_bindings.py`, `windows_probe_bindings.py` | `execution.py`, `validation_commands.py`, `windows_validation_script.py`, `targets.py`, `target_preflight.py`, `ssh_subprocess.py`, `ssh_bundle.py`, `linux_target.py`, `windows_target.py`, `windows_probe.py` |
| CLI dispatch and shared helpers | `cli_parser_bindings.py`, `cli_dispatch_bindings.py`, `cloud_bindings.py`, `config_evidence_bindings.py`, `evidence_index_bindings.py`, `github_workflow_bindings.py`, `notification_bindings.py`, `normalize_bindings.py`, `provenance_bindings.py`, `git_helpers_bindings.py`, `io_utils_bindings.py`, `footprint_bindings.py`, `state_path_bindings.py` | `cli_parser.py`, `cli_dispatch.py`, `cloud.py`, `evidence_index.py`, `github_workflows.py`, `notifications.py`, `normalize.py`, `provenance.py`, `git_helpers.py`, `io_utils.py`, `footprint.py`, `state_paths.py` |

## Behavior Contracts

The contract tests in `test_local_ci_contracts.py` pin the seams that future
extractions must preserve:

- Queue/evidence: exact-SHA jobs dedupe by branch/SHA/targets/validation, a
  newer SHA supersedes older pending work in the same scope, and evidence keeps
  only latest passing per-target records.
- Target preflight: unreachable SSH targets either fail clearly, remain queued
  only with `--allow-unreachable-targets`, or fail over to Namespace when that
  provider is configured.
- Source preparation: source cache keys include SHA and prepare command, launch
  commands rewrite repo-relative executables into the prepared source root, and
  prepared state is target/mode scoped.
- Cleanup: live queue artifacts are retained, orphan retention counts are
  honored, and prepared-state cleanup is opt-in.
- Artifact publishing: desktop run/publish bundles keep their stable directory
  shape, and branch publishing reports deterministic paths and URLs without
  depending on live network operations in tests.
