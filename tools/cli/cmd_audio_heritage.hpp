#pragma once

#include <string>
#include <vector>

/// Authoring, inspection, and deterministic fixture rendering for versioned
/// sampler Heritage profiles.
int cmd_audio_heritage(const std::vector<std::string>& args);
