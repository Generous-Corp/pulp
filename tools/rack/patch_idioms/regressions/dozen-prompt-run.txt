# A dozen prompts, run end to end against the library.
#
# Kept as evidence rather than a claim. Eleven held; the twelfth ran out of
# attempts at the audio gate with the patch the right shape and silent.
#
# The previous run of this file recorded six. What moved was not the model:
# three of the six failures were the checker rejecting correct patches, for
# reasons that had nothing to do with the patch. Fundamental's ADSR was not
# recognised as an envelope because its tag is capitalised differently from
# Rack's canonical spelling; two thirds of Fundamental's modules and all of
# Core's carried no port roles, so any requirement naming a port kind was
# unsatisfiable; and a module nobody had cartographed REFUTED a requirement
# rather than leaving it unproven, which made the only quantizer a stock Rack
# has into a quantizer no patch could route through.
#
# The classification below is counted from the transcripts rather than
# remembered. The previous version of this file said four prompts died at the
# audio gate when its own transcript showed two -- only the LAST attempt's
# reason survived, and the earlier ones had scrolled away.
#
# Re-run with: tools/rack/prove_idioms.sh
#
 1. generative a krell patch where each note chooses the next one's -> krell (named)
    HOLDS  a-krell-patch-where-each-note-chooses-th-3.vcv
 2. generative an evolving ambient drone that never settles         -> wandering-drone (named)
    HOLDS  an-evolving-ambient-drone-that-never-set-3.vcv
 3. generative a melody that repeats then slowly mutates            -> turing-machine (implied)
    HOLDS  a-melody-that-repeats-then-slowly-mutate-2.vcv
 4. rhythm    a bouncing ball rhythm that slows down as it settles -> bouncing-ball (named)
    HOLDS  a-bouncing-ball-rhythm-that-slows-down-a-4.vcv
 5. rhythm    a kick drum with a fast pitch sweep                  -> kick-drum (named)
    FAILED to build:     - something has to trigger it
        attempts stopped by: 4 silent, 0 rejected by the lint, 3 wrong idiom
 6. rhythm    four different speeds coming from one clock          -> generative-clock-divider (implied)
    HOLDS  four-different-speeds-coming-from-one-cl-3.vcv
 7. voice     a classic subtractive voice with a filter envelope   -> subtractive-voice (named)
    HOLDS  a-classic-subtractive-voice-with-a-filte-5.vcv
 8. voice     a west coast voice through a low pass gate           -> west-coast-voice (named)
    HOLDS  a-west-coast-voice-through-a-low-pass-ga-4.vcv
 9. voice     a bass sound that stays in the key of C              -> quantized-voice (implied)
    HOLDS  a-bass-sound-that-stays-in-the-key-of-c.vcv
10. modulation slow vibrato on a sustained note                     -> vibrato (named)
    HOLDS  slow-vibrato-on-a-sustained-note-4.vcv
11. texture   wind and rain that keeps shifting                    -> noise-texture (implied)
    HOLDS  wind-and-rain-that-keeps-shifting-4.vcv
12. texture   two detuned oscillators beating against each other   -> drone-cluster (named)
    HOLDS  two-detuned-oscillators-beating-against--2.vcv

11 held, 1 did not
across every attempt of the runs that failed: 4 silent, 0 rejected by the lint, 3 wrong idiom
full transcripts: /private/tmp/claude-501/-Volumes-Workshop-Code-pulp/ae6b385d-a5c2-474e-848e-539c140a5c74/scratchpad/idiom-proof-2-logs
