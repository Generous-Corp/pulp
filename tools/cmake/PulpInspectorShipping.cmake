# Compatibility include for projects that consumed the original helper name.
# Capability-control shipping policy and enforcement live in the generic module.
include("${CMAKE_CURRENT_LIST_DIR}/PulpControlShipping.cmake")
