#include <pulp/signal/spectral_cross_synthesis.hpp>

static_assert(pulp::signal::SpectralCrossSynthesis::added_latency_samples() == 0);
static_assert(pulp::signal::SpectralCrossSynthesis::added_tail_samples() == 0);
