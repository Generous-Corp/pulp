"Phase 4: Adoption & Integration — docs, migration guides, examples, ecosystem.

Reference: Phase 4 planning doc (generated by agent)

PRIORITY ORDER:

1. GETTING STARTED GUIDE — docs/getting-started.md
   Step-by-step: install, create plugin, build, hot-reload, first knob, first theme.
   Include terminal commands and expected output at each step.

2. FROM REACT/CSS MIGRATION GUIDE — docs/from-react-css.md
   Side-by-side mapping tables: React component -> Pulp widget, CSS property -> bridge.
   Common patterns: state management, event handling, animation, layout.
   What breaks, what transfers, what's different.

3. API REFERENCE GENERATION — docs/api/
   Extract all JS bridge functions into a structured reference.
   Group by category: layout, styling, widgets, events, canvas, animation, theme.
   Include parameters, return values, and one-line examples.

4. COOKBOOK — docs/cookbook.md
   10 recipes: form layout, modal dialog, scrolling list, tab panel, custom meter,
   theme switching, hover effects, keyboard shortcuts, drag interaction, preset browser.

5. STARTER TEMPLATE — tools/cli/templates/
   'pulp create MyPlugin --template gain' scaffolds a minimal plugin.
   Includes: CMakeLists.txt, main.cpp, ui.js, theme.json, README.
   Build and run instructions that work out of the box.

6. COMPONENT SHOWCASE — examples/showcase/
   Single JS file that demonstrates every widget type.
   Screenshot-testable. Serves as visual regression baseline.
   Shows: knobs, faders, toggles, meters, spectrum, waveform, XY pad,
   combo, text input, tabs, scroll, modal, tooltip, progress, checkbox.

7. DESIGN TOKEN GUIDE — docs/design-tokens.md
   How tokens work, how to create theme.json, how inheritance flows,
   how to export to CSS/WGSL, how to integrate with Figma.

8. CUSTOM RENDERING GUIDE — docs/custom-rendering.md
   Layer B: CanvasWidget draw commands from JS.
   Layer C: custom View subclass with Canvas API in C++.
   Layer C+: Dawn/WebGPU access for shader-driven visuals.
   When to use each level.

TESTING: Verify docs build/render, starter template compiles, showcase screenshots.
EACH ITERATION: write one doc/example, verify, commit.
COMPLETION: Output 'PHASE 4 COMPLETE' when all 8 items exist." --completion-promise "PHASE 4 COMPLETE" --max-iterations 120
