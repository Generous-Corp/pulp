// Hot-Reload Synth — CLAP entry point (instrument via the descriptor).
#include "hot_reload_synth_shell.hpp"
#include <pulp/format/clap_entry.hpp>

PULP_CLAP_PLUGIN(pulp::examples::create_hot_reload_synth)
