# Cloudflare Pages — GENERATED by pulp-web-demo v{{GENERATOR_VERSION}} (owned file).
# Deploy:  wrangler pages deploy {{OUTPUT_DIR}} --project-name {{PROJECT_NAME}}
# The _headers file in {{OUTPUT_DIR}} supplies COOP/COEP/CORP; Wrangler serves it verbatim.
name = "{{PROJECT_NAME}}"
pages_build_output_dir = "{{OUTPUT_DIR}}"
compatibility_date = "2024-11-01"
