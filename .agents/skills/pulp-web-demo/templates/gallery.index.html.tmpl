<!doctype html>
<!-- GENERATED by pulp-web-demo generator v{{GENERATOR_VERSION}} — owned file. -->
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
<title>{{GALLERY_TITLE}}</title>
<meta property="og:title" content="{{GALLERY_TITLE}}" />
<meta property="og:type" content="website" />
<meta property="og:url" content="{{GALLERY_URL}}" />
{{THEME_LINKS}}
<style>
  body { font-family: var(--font-family-native, system-ui); color: var(--text-primary, #111);
         background: var(--bg-primary, #fff); margin: 0; padding: 2rem; }
  h1 { font-size: 1.4rem; }
  .cross { margin: .5rem 0 1.5rem; }
  .cross a { color: var(--accent-primary, #06f); }
  .{{LAYOUT_CLASS}} { display: grid; gap: 1rem; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); }
  .card { border: 1px solid var(--control-border, #ddd); border-radius: 10px; padding: 1rem; }
  .card h2 { font-size: 1.05rem; margin: 0 0 .25rem; }
  .card p { color: var(--text-secondary, #555); font-size: .9rem; margin: 0 0 .75rem; }
  .abis a { display: inline-block; margin-right: .75rem; color: var(--accent-primary, #06f); }
</style>
</head>
<body>
<h1>{{GALLERY_TITLE}}</h1>
<div class="cross">{{CROSS_LINKS}}</div>
<div class="{{LAYOUT_CLASS}}">
{{PLUGIN_CARDS}}
</div>
</body>
</html>
