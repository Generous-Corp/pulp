{
  "_comment": "Pulp MCP server for both source-tree and marketplace-plugin use. The CLI installers persist PULP_MCP_BINARY; source contributors can set it to their checkout build.",
  "mcpServers": {
    "pulp": {
      "command": "${PULP_MCP_BINARY}",
      "env": {}
    }
  }
}
